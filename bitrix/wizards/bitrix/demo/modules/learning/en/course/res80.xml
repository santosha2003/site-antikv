<?xml version="1.0" encoding="windows-1251"?>
<content type="LES"><id>388</id>
<timestamp_x>1162482874</timestamp_x>
<date_create>1153745236</date_create>
<created_by>1837</created_by>
<active>Y</active>
<course_id>10</course_id>
<chapter_id>145</chapter_id>
<name>Navigation chain template management</name>
<sort>300</sort>
<preview_picture></preview_picture>
<preview_text></preview_text>
<preview_text_type>text</preview_text_type>
<detail_picture></detail_picture>
<detail_text><![CDATA[
<p>A template that is used to display the navigation chain is defined in the file <code>/bitrix/templates/<template name=""></template>/chain_template.php</code> for each site template individually. The structure of a navigation chain template is similar to that of a menu template. The template includes:</p>

<ul>
  <li>a header; </li>

  <li>a body element; </li>

  <li>a footer parts. </li>
</ul>

<p>Body element defines a template used to display a single element. The whole chain is built in a loop. Sample navigation chain template:</p>

<pre class="syntax">&lt;?
//--- Chain header
$sChainProlog=&quot;&quot;;

//--- Body element 
 $sChainBody = &quot;&quot;;
//--- The variable $ITEM_INDEX contains the current element number.
if($ITEM_INDEX &gt; 0)
$sChainBody = &quot; &nbsp;&raquo;&nbsp;&quot;;
$sChainBody .= &quot;&lt;a href=&quot;&quot;.$LINK.&quot;&quot; class=&quot;smalltext&quot;&gt;&quot;.htmlspecialchars($TITLE).&quot;&lt;/a&gt;&quot;;


//--- Footer
$sChainEpilog=&quot;&quot;;
?&gt;</pre>

<p>You can quickly switch to editing the navigation chain template by clicking the button&nbsp;<img src="cid:resources/res80/V5Ndz.VdtZj.area_sm_edit_chain.gif" height="22" width="22"/> while in the editable area display mode. 
  <br />

  <br />
<img src="cid:resources/res80/sU75G.HcLKm.templ_ed.png" height="158" width="484"/> 
  <br />

  <br />
You can set the individual navigation chain template for a separate site section. To do so, define a variable <code>$sChainTemplate</code> in the file <code>.section.php</code> and assign it a string value containing the full path to the navigation chain template. For example:</p>

<pre class="syntax">$sChainTemplate = &quot;/bitrix/templates/demo/chain_template.php&quot;</pre>

<p>The navigation chain template also can be specified as one of parameters of the function <code>ShowNavChain()</code>.</p>

<pre class="syntax">$APPLICATION-&gt;ShowNavChain(&quot;/bitrix/templates/.default/chain_template_bottom.php&quot;)</pre>

<p></p>

<div class="hint"><b>Note</b>: Trial version of the product contains the navigation chain template in the default site template folder: <code>/bitrix/templates/.default/</code>.</div>

<p></p>
]]></detail_text>
<detail_text_type>html</detail_text_type>
</content>